{
  "name": "mahjong-match-game",
  "version": "1.0.0",
  "description": "麻将消消乐 - 跨平台游戏",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:init": "npx cap init",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open:android": "npx cap open android",
    "build:apk": "npm run build && npm run cap:sync && npm run cap:open:android"
  },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/android": "^6.0.0"
  }
}